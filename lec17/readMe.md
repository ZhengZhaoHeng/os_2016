##Eisenberg算法实现说明

1. 我们对Eisenberg算法的代码部分进行抽象，将它们编成一条一条的汇编指令形式
2. 我们通过current数组来记录每个进程执行到第几条指令
3. 用switch分支模拟进程内部一条一条指令的顺序执行和跳转，将下一条指令位置存在current数组内
4. 对于flag标记，我们设置3种状态，IDLE表示当前进程不想进入临界区，ACTIVE表示当前进程想要进入临界区，WAIT表示当前进程想要进入临界区但turn并未轮到自己
5. 在进入临界区的前、中、后，我们分别输出当前进程的pid，如果这3者的pid相同，说明我们成功实现了软件同步的目标